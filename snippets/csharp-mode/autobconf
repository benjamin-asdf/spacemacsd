# -*- mode: snippet -*-
#name : autobconf
#key : autobconf
#contributor : benj

# --


    public static `class-name` With`field-name-capital`(this AutoBuyRewardConfig config, `type` `field-name`) {
        config.`field-name` = `field-name`;
        return config;
    }
