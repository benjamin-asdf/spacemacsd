# -*- mode: snippet -*-
#name : autobconf
#key : autobconf
#contributor : benj

# --


    public static `class-name` With`field-name-capital``generic-type-part`(this `class-name` config, `type` `(concat field-name (if (string-equal type "bool") " = true" ""))`)`type-constraint-part` {
        config.`field-name` = `field-name`;
        return config;
    }
