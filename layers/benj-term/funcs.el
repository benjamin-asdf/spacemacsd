
;; TODO


;;(with-eval-after-load 'evil-core
 ;; (evil-set-initial-state 'term-mode 'emacs))

;; (evil-set-initial-state 'term-mode 'emacs)

;; (add-hook )

;; not how it works
;; (add-hook 'window-selection-change-functions #'(lambda (window) (message (buffer-name (window-buffer window)))))
