;; (defconst benj-ediff-leader-keys "oM")

;; (spacemacs/declare-prefix benj-ediff-leader-keys "merge-ediff")

;; (progn
;;   (mapc (lambda (x)
;;           (spacemacs/set-leader-keys (concat benj-ediff-leader-keys (car x)) (cdr x)))
;;         '(("" . )
;;           )))
